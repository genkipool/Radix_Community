// We don't have Rust / cargo set up with radix-engine dependencies here easily.
// But we have radix-engine-toolkit.
// Maybe I can just use RET staticallyValidate with Enum<1u8>(0u64) instead of Enum<1u8>("DataSchema").
